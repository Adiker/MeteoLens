# Base URL of the MeteoLens backend the browser should call.
# Defaults to http://localhost:8000. Override it if that port is taken by another
# app (e.g. run the backend on 8010 and set the line below), then restart `npm run dev`.
VITE_API_BASE_URL=http://localhost:8000
