export function AttributionBar() {
  return (
    <footer className="flex min-h-9 flex-wrap items-center justify-between gap-2 border-t border-border bg-card px-4 py-2 text-xs text-muted-foreground">
      <span>Źródło danych: IMGW-PIB.</span>
      <span>Dane IMGW-PIB będą przetwarzane przez MeteoLens po uruchomieniu parserów.</span>
    </footer>
  );
}

